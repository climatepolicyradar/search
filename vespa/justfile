# Extract the data to load into Vespa
extract-documents:
    aws s3 cp s3://cpr-cache/search/materialize_vespa_updates/from_data_in_api.jsonl ../.data_cache/materialize_vespa_updates/from_data_in_api.jsonl

feed-documents limit="":
    if [ -z "{{limit}}" ]; then \
        echo "Feed all files into local Vespa"; \
        cat ../.data_cache/materialize_vespa_updates/from_data_in_api.jsonl | vespa feed - --target http://localhost:8080 --progress 30; \
    else \
        echo "Feed {{limit}} files into local Vespa"; \
        head -n {{limit}} ../.data_cache/materialize_vespa_updates/from_data_in_api.jsonl | vespa feed - --target http://localhost:8080 --progress 30; \
    fi

extract-concepts:
    aws s3 cp s3://cpr-cache/search/materialize_vespa_updates/from_inference_results.jsonl ../.data_cache/materialize_vespa_updates/from_inference_results.jsonl

feed-concepts:
    vespa feed ../.data_cache/materialize_vespa_updates/from_inference_results.jsonl --target http://localhost:8080 --progress 30

extract-labels:
    aws s3 cp s3://cpr-cache/search/vespa/labels_feed_materializer.jsonl ../.data_cache/vespa/labels_feed_materializer.jsonl

feed-labels:
    vespa feed ../.data_cache/vespa/labels_feed_materializer.jsonl --target http://localhost:8080 --progress 30

extract-passages:
    aws s3 cp s3://cpr-cache/search/vespa/passages_feed_materializer.jsonl ../.data_cache/vespa/passages_feed_materializer.jsonl

feed-passages:
    vespa feed ../.data_cache/vespa/passages_feed_materializer.jsonl --target http://localhost:8080 --progress 30

# Run vespa dev
up:
    docker compose up -d

# Deploy Vespa application
deploy:
    vespa deploy ./app --target http://localhost:19071/ --wait 300

# Load data into Vespa
feed limit="":
    if [ -z "{{limit}}" ]; then \
        echo "Feed all files into local Vespa"; \
        cat ../.data_cache/materialize_vespa_updates/from_data_in_api.jsonl | vespa feed - --target http://localhost:8080 --progress 30; \
        cat ../.data_cache/materialize_vespa_updates/from_indexer_input.jsonl | vespa feed - --target http://localhost:8080 --progress 30; \
    else \
        echo "Feed {{limit}} files into local Vespa"; \
        head -n {{limit}} ../.data_cache/materialize_vespa_updates/from_data_in_api.jsonl | vespa feed - --target http://localhost:8080 --progress 30; \
        head -n {{limit}} ../.data_cache/materialize_vespa_updates/from_indexer_input.jsonl | vespa feed - --target http://localhost:8080 --progress 30; \
    fi

# Dev
dev:
    just up
    just deploy
    just feed 100

get query="congo":
    #!/usr/bin/env bash
    set -e
    curl -G "http://localhost:8080/search/" \
     --data-urlencode 'yql=select * from sources documents where userQuery()' \
     --data-urlencode 'query={{query}}' \
     --data-urlencode 'hits=10'

get-document id="CCLW.collection.1755.0":
    vespa document get "id:documents:documents::{{id}}"

# Remove the application. To remove data as well, use `reset` below.
destroy:
    curl -X DELETE http://localhost:19071/application/v2/tenant/default/application/default

# Remove the Vespa container and volume
reset:
    docker compose down -v

# remote command
remote-deploy:
    #!/usr/bin/env bash
    set -e
    # read this parameter from aws smm and store it in a variable it's a secret
    vespa_endpoint=$(aws ssm get-parameter --name "/search/vespa/endpoint" --query "Parameter.Value" --output text --with-decryption)
    vespa_application=$(aws ssm get-parameter --name "/search/vespa/application" --query "Parameter.Value" --output text --with-decryption)
    vespa prod deploy ./app --target cloud --application $vespa_application --zone prod.aws-eu-west-1a

remote-feed-documents:
    #!/usr/bin/env bash
    set -e
    vespa_write_token=$(aws ssm get-parameter --name "/search/vespa/write_token" --query "Parameter.Value" --output text --with-decryption)
    vespa_endpoint=$(aws ssm get-parameter --name "/search/vespa/endpoint" --query "Parameter.Value" --output text --with-decryption)
    vespa feed ../.data_cache/materialize_vespa_updates/from_data_in_api.jsonl --target $vespa_endpoint --header "Authorization: Bearer $vespa_write_token" --progress 30

remote-feed-concepts:
    #!/usr/bin/env bash
    set -e
    vespa_write_token=$(aws ssm get-parameter --name "/search/vespa/write_token" --query "Parameter.Value" --output text --with-decryption)
    vespa_endpoint=$(aws ssm get-parameter --name "/search/vespa/endpoint" --query "Parameter.Value" --output text --with-decryption)
    vespa feed ../.data_cache/materialize_vespa_updates/from_inference_results.jsonl --target $vespa_endpoint --header "Authorization: Bearer $vespa_write_token" --progress 30

remote-feed-passages:
    #!/usr/bin/env bash
    set -e
    vespa_write_token=$(aws ssm get-parameter --name "/search/vespa/write_token" --query "Parameter.Value" --output text --with-decryption)
    vespa_endpoint=$(aws ssm get-parameter --name "/search/vespa/endpoint" --query "Parameter.Value" --output text --with-decryption)
    vespa feed ../.data_cache/materialize_vespa_updates/from_indexer_input.jsonl --target $vespa_endpoint --header "Authorization: Bearer $vespa_write_token" --progress 30

remote-visit:
    #!/usr/bin/env bash
    set -e
    vespa_read_token=$(aws ssm get-parameter --name "/search/vespa/read_token" --query "Parameter.Value" --output text --with-decryption)
    vespa_endpoint=$(aws ssm get-parameter --name "/search/vespa/endpoint" --query "Parameter.Value" --output text --with-decryption)
    mkdir -p ../.data_cache/vespa
    vespa visit --target "$vespa_endpoint" --header "Authorization: Bearer $vespa_read_token" > "../.data_cache/vespa/documents-cloud.jsonl"
    echo "Exported to ../.data_cache/vespa/documents-cloud.jsonl"
